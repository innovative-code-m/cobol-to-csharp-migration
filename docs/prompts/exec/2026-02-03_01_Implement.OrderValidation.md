# Exec: 01_Implement – OrderValidation

> 保存先：`docs/prompts/exec/2026-02-03_01_Implement_OrderValidation.md`
>
> このファイルは **実行ログ（契約ログ）** です。AI に投入した内容と一致するように記録します。

---

## 対象ファイル

- 実装対象：
  - `src/OrderService.cs`
- テスト対象：
  - `tests/OrderServiceTests.cs`

---

## 変更目的

注文金額が 0 以下の場合に、例外ではなく `false` を返すようにする。

---

## 受入基準（すべて必須）

- [ ] 金額 > 0 の場合、従来どおり `true` を返す
- [ ] 金額 <= 0 の場合、例外を投げず `false` を返す
- [ ] 既存テストがすべて成功する
- [ ] 新規/更新テストが追加され、今回の変更を検証できる

---

## 禁止事項（今回固有の追加があれば記入）

- public API 変更禁止
- 例外型の追加禁止
- 新規依存ライブラリ追加禁止

※ ここに書かない場合でも、下記テンプレ（01_Implement）の禁止事項は常に適用される。

---

## 使用テンプレート

- `docs/prompts/dev/01_Implement.prompt.md`

---

## 実行用プロンプト本文（AIに投入する“全文”）

> ✅ ここから下を **そのまま Copilot Chat（Codex想定）** に貼り付けて実行します。
> 実行後、AIが返した差分（patch）も別ファイルで保存するとより良いです（任意）。

---

以下のプロンプトに従って実装してください。

【最優先条件（本実行固有・テンプレより優先）】
以下は今回の実行における確定条件であり、テンプレ内のプレースホルダ（`src/...` 等）は使用しないこと。

【対象ファイル】
- 実装対象：`src/OrderService.cs`
- テスト対象：`tests/OrderServiceTests.cs`

【変更目的】
注文金額が 0 以下の場合に、例外ではなく `false` を返すようにする。

【受入基準】
- 金額 > 0 の場合、従来どおり `true` を返す
- 金額 <= 0 の場合、例外を投げず `false` を返す
- 既存テストがすべて成功する
- 新規/更新テストが追加され、今回の変更を検証できる

【禁止事項（今回固有）】
- public API 変更禁止
- 例外型の追加禁止
- 新規依存ライブラリ追加禁止

---

（ここに `docs/prompts/dev/01_Implement.prompt.md` の全文を貼り付ける）

---

## 実行結果メモ（任意）

- 実行したAI：Copilot Chat（Codex想定）
- 実行日時：2026-02-03
- 結果：成功 / 要修正
- 修正が必要だった点：
  -

---

以上

